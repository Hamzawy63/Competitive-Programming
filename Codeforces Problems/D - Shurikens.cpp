#include <bits/stdc++.h>
 
using namespace std;
#define space " "
using ll =  long long int;
 
void solve() {
    vector<int> a(6);
    for (int i = 0; i < 6; i++) {
        cin >> a[i];
    }
    int n;
    cin >> n;
    vector<int> b(n);
    for (int i = 0; i < n; i++) {
        cin >> b[i];
    }
    vector<pair<int, int>> all;
    for (int i = 0; i < n; i++) {



















































