#include <bits/stdc++.h>
using namespace std; 
void solve() {
    int n ; 
    cin >> n ;
    cout << n  << endl; 
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0); // FAST IO
    int q = 1;
    cin >> q;
    for (int i = 1; i <= q; i++) {
        // cout << "Case #" << i << ": ";
        solve();
    }
}